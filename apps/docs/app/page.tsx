import { Button } from "@repo/ui/components/ui/button";
import { type NextPage } from "next";

const Page: NextPage = () => {
  return (
    <main>
      <Button>Click me</Button>
    </main>
  );
};

export default Page;
